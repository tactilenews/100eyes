<%= form_with(
  model: @contributor,
  hidden: true,
  local: true,
  url: onboarding_path(jwt: jwt),
  class: class_attr,
) do %>
  <%= c 'stack' do %>
    <%= c 'field', object: @contributor, id: :first_name do |field| %>
      <%= c 'input', field.input_defaults %>
    <% end %>

    <%= c 'field', object: @contributor, id: :last_name do |field| %>
      <%= c 'input', field.input_defaults %>
    <% end %>

    <%= c 'field', object: @contributor, id: :email do |field| %>
      <%= c 'input', field.input_defaults.merge(placeholder: '') %>
    <% end %>

    <%= c 'button',
      type: 'submit',
      label: I18n.t('components.onboarding_email_form.submit'),
      style: :primary
    %>
  <% end %>
<% end %>
