<div
  class="CreateInviteButton"
  data-controller="create-invite-button"
  data-action="click->create-invite-button#copyLink"
>
  <%= c 'button', style: :inline, stimulus_target: 'create-invite-button.button' do %>
    <span data-target="create-invite-button.label">
      <%= t 'components.create_invite_button.label' %>
    </span>
    <span data-target="create-invite-button.loading" hidden>
      <%= t 'components.create_invite_button.loading' %>
    </span>
    <span
      class="CreateInviteButton-success"
      data-target="create-invite-button.success"
      hidden
    >
      <%= c 'icon', icon: 'f-check', style: :inline %>
      <%= t 'components.create_invite_button.success' %>
    </span>
  <% end %>
</div>
