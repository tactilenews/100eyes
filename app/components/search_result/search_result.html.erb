  <% case type %>
  <% when :user %>
    <%= c 'user_row', user: result %>
  <% else %>
    <%= c 'excerpt', title: result.user.name, text: result.text, link: user_request_path(user_id: result.user_id, id: result.request_id), date: result.created_at %>
  <% end %>
