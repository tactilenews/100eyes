<%= form_with(scope: :contributor, url: message_organization_contributor_path(organization, contributor), class: 'ChatForm', id: 'chat-form', local: true) do %>
  <% if reply_to.present? %>
    <%= hidden_field_tag 'message[reply_to_id]', reply_to&.id %>
  <% end %>
  <%= c 'textarea', id: 'message[text]', required: true, placeholder: t('components.chat_form.placeholder') %>
  <%= c 'button', label: t('components.chat_form.submit'), type: :submit %>
<% end %>
