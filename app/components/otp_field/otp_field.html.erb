<%= c('base_field',
  label: t('.label'),
  help: t('.help'),
  **attrs
) do |field| %>
  <%= c 'input',
    required: true,
    minlength: 6,
    maxlength: 6,
    inputmode: 'numeric',
    **attrs.slice(:id)
  %>
<% end %>
