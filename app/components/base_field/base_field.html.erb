<div <%= attrs %>>
  <div class="BaseField-textWrapper <%= hide_label ? 'visually-hidden' : '' %>">
    <label
      class="BaseField-labelText"
      for="<%= id %>"
      id="label-<%= id %>"
    >
      <%= label %>
    </label>

    <% if help %>
      <p class="BaseField-helpText"><%= help %></p>
    <% end %>

    <% if errors.any? %>
      <% errors.each do |error| %>
        <strong class="BaseField-errorText">
          <%= error %>
        </strong>
      <% end %>
    <% end %>
  </div>

  <%= content %>
</div>
