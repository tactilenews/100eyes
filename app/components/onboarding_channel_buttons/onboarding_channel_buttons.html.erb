<div <%= attrs %>>
  <% channels.each do |channel| %>
    <%= c 'button',
      link: send("onboarding_#{channel}_path"),
      styles: [:primary] do
    %>
      <%= svg "channel_#{channel}" %>
      <%= t(".#{channel}") %>
    <% end %>
  <% end %>
</div>
