<%= c 'button', **attrs
  .defaults(type: :submit)
  .merge(
    styles: styles,
    data: {
      controller: 'submit-button',
      action: '
        click->submit-button#setLoadingState
        turbo:before-cache@window->submit-button#resetLoadingState
      ',
    }
  )
%>
